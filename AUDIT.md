# AUDIT — 100マス計算（GIGA Standard v5）

- 対象: `GIGAyama/online-100square-calculation`
- 型: **B型**（Vite + React / GitHub Pages）
- 監査日: 2026-08-05（**第2回**。第1回は 2026-08-04 / `v1.4.0`）
- 版: `1.4.0` → **`1.5.0`**

## 測りかた（推測で書かないための前提）

- **実ブラウザ（Chromium 141）で開いて測った。** 静的に読んだだけの項目は「未計測」と書く。
- コントラストは §7-2 のとおり、色を **1px 実際に塗って `getImageData` で読む**方法で測った。
- 測った画面は **初期 / プレイ中 / 結果 / 設定 / 記録 / 中断かくにん** ×
  **320×568・375×667・810×1080・1366×768**。記録画面は記録が入った状態も作って測った。
- 作業環境からは `fonts.googleapis.com` へ出られない。
  これは**学校のフィルタリングと同じ状態**なので、そのまま利点として使った（§7-4）。
- 「改修前」の数字は、**改修前のコードを実際にビルドして同じ手順で測った値**である（§7-6）。

---

## この回で分かったこと

第1回（`v1.4.0`）で **「CSS は入れたが実物は未計測」と書いて残した3項目を、実際に測った。**
測ったら、**そのうち2つは壊れていた。**

> 「未計測」は ✅ ではない、というのは本当だった。
> どちらも画面を見ているかぎり絶対に気づけない壊れ方をしていた。

### 1. 印刷すると、いちばん右の列が切れていた（D13）

先生が白紙のマスを刷って配る使い方は MANUAL.md に書いてあり、CSS も書いてあった。
**A4 縦の印刷可能幅（186mm＝703px）で実際に組むと、表が入りきっていなかった。**

原因は `@media print` ではなく、画面用の `overflow-x: auto` だった。
**画面では「はみ出したら横スクロール」だが、紙には横スクロールが無い。はみ出した分はそのまま切り取られる。**

| | 改修前 | 改修後 |
|---|---:|---:|
| 表の右端（印刷可能幅 703px に対して） | 692.2px | **685.1px** |
| 10列目の切れ | **13.2px（約3.5mm）** | **0**（6.9px の余り） |

1366px の viewport のまま `print` メディアだけ当てても再現しない。
**紙の幅で組み直さないと見えない**（第1回はこれを取りこぼしていた）。

### 2. 手書き入力の枠が紙に出て、A4 の 1/4 を食っていた（D13）

`@media print` で `nav` / `footer` / `button` は消していたが、
**手書き入力のパネルそのものには `.no-print` が付いていなかった。**
紙の上では手書きもテンキーも押せないので、まるごと無駄になる。

| | 改修前 | 改修後 |
|---|---:|---:|
| 手書き入力パネルの印刷高さ | **261px（約69mm）** | **0** |

### 3. ハイコントラストで「まちがい」の手がかりが1つも残っていなかった（D11）

`@media (forced-colors: active)` は書いてあった。しかし**受けていたのは `.cell-correct` だけ**で、
`.cell-wrong` には何も無かった。

ハイコントラストでは面の色（緑・赤）が**両方とも**消えるため、こうなる。

| マス | 改修前（ハイコントラスト） | 改修後 |
|---|---|---|
| 正解 | 太い枠 | 太い**実線**の枠 |
| **まちがい** | **手がかりなし＝未回答のマスと同じ見た目** | 太い**破線**の枠 |
| 未回答 | 細い枠 | 細い枠 |

**片方だけ手当てすると、差が消えたことに気づけない。**
比較対象が画面に出ないので、スクリーンショットを見ても違和感が出ないためである。

あわせて、**ふつうの表示でも正誤を色だけで伝えていた**（緑の面／赤の面のみ）ので、
実線・破線の枠を通常時にも付けた（§2-8「色だけで意味を伝えない」）。
`border` ではなく `outline` を使っている。`border` を足すと数字が1〜2px ずれて 100マスがガタつくため。

**見分けられる状態の数：ハイコントラスト 1/3 → 3/3、ふつう 2/3 → 3/3。**

### 4. 提示モード（D12）— 第1回で ❌ のまま残していたものを実装した

第1回は「先生の使い方を確認したうえで判断したい」として入れていなかった。
100マス計算は**先生の合図で一斉に始める**使い方が中心で、§2-11 は
「一斉授業で使うアプリには必ず用意する」としているため、今回入れた。

実装して測ってわかったことが2つある。

**（a）文字だけ大きくしても、マスは大きくならない。**
本文は `max-w-6xl`（1152px）で止めてあるため、**3840px の電子黒板に映しても表は 1152px のまま**で、
マスは 69.8px から1px も動かなかった。提示モードでは幅の上限も外す必要がある。

**（b）主役の文字を画面幅だけで大きくすると、列からはみ出す。**
v5 §2-11 のひな形どおり `--fs-hero: clamp(64px, 8vw, 160px)` を当てたところ、
計算式は幅 320px 固定の入力ツール列の中にあるため、**3840px の画面で 372px はみ出し、横スクロールが出た**。
列を広げてから、その幅に収まる範囲で大きくする形にした。

| 画面 | マス（`--cell`） | マスの数字 | 計算式 | 横スクロール |
|---|---:|---:|---:|---:|
| 1366×768 ふつう | 56px | 23.5px | 48px | 0 |
| 1366×768 提示 | 56px | **28px** | 48px | 0 |
| 3840×2160 ふつう | 56px | 23.5px | 48px | 0 |
| 3840×2160 提示 | **104px** | **52px** | **84px** | **0** |

320px〜3840px の7サイズすべてで横スクロール 0・JS エラー 0・コントラスト基準未満 0 を確認した。
**キーボードだけでも入れる／戻れる**（Tab → Enter）ことと、
**Esc やブラウザ側で全画面を解除したときに表示も戻る**ことを実測した。

### 5. 品質ゲートの自己テストが、検査の不具合を2件見つけた

§P4 のとおり「わざと壊して通ることを確認」したところ、**今回足した検査自体が2件壊れていた。**

| 見つかった不具合 | 中身 |
|---|---|
| `FORCED_COLORS_PAIR` の誤検知 | 接頭辞が同じというだけで対にすると、`.cell-error-flash`（動きだけを付ける規則）まで仲間に数える。**色を宣言しているクラスに限る**のが正しい |
| `PRINT_SCROLL_CLIP` が何も見ていなかった | スクロールさせているのは JSX 側の Tailwind クラス（`overflow-x-auto`）で、`@media print` はスタイルシートにある。**別々のファイルなので、1ファイルずつ見ると永久に一致せず「通ってしまう」** |

後者は「0件でした」としか出ないため、自己テストが無ければ**入れた検査が一度も働かないまま**残っていた。

---

## 実測（改修前 `v1.4.0` → 改修後 `v1.5.0`）

| 項目 | 前 | 後 |
|---|---:|---:|
| **印刷：表の切れ** | **13.2px（約3.5mm）** | **0** |
| **印刷：紙に出る画面用パネル** | **261px（約69mm）** | **0** |
| **ハイコントラストで見分けられる状態** | **1/3** | **3/3** |
| **ふつうの表示で見分けられる状態** | 2/3 | **3/3** |
| **提示モード** | **無し** | マス 56→**104px**／数字 23.5→**52px** |
| コントラスト基準未満（6画面×4サイズ のべ） | 0件 | **0件** |
| タップ44px未満（計算マス以外） | 0件 | **0件** |
| タップ44px未満（計算マス・320/375px のみ） | 40件 | 40件（※） |
| 横スクロール（320px〜3840px） | 0件 | **0件** |
| CSP 違反 | 0件 | **0件** |
| JS エラー | 0件 | **0件** |
| 品質ゲートの検査数 | 16 | **19**（すべて自己テストで反応を確認） |
| 初回表示に必要な JS | 206.9 KB (gzip 63.9) | 208.9 KB (gzip **64.8**) |
| 先読みキャッシュ | 798.3 KB | 801.3 KB |

※ 計算マスは第1回と同じ理由で直していない（後述）。

### PWA の挙動（§7-5・すべて再実測）

| 確かめたこと | 結果 |
|---|---|
| Service Worker が実際に登録されている | ✅ `activated` |
| **初回訪問で勝手にリロードしない** | ✅ 画面遷移 **1回** |
| **押すまで切り替わらない**（版を上げて3秒放置） | ✅ `waiting` のまま・画面遷移 **0回**・おしらせは表示 |
| 押したら切り替わる | ✅ 画面遷移1回、`square100-static-v1.5.0` が消えて新版になった |
| **他アプリのキャッシュを巻き添えにしない** | ✅ `townmap-mikke-static-v2` / `keisan-card-runtime-v1` は残った |
| 圏外で起動する | ✅ 表まで出る |
| 本体の控えを消すと `offline.html` が出る | ✅ 「いま インターネットに つながっていません」 |
| TensorFlow.js が先読みに入っていない | ✅ |

### キーボードだけで全機能に到達できるか（再実測）

```
Tab 順: 計算の種類 → 問題数 → スタート！ → 大きく表示（提示モード）
        → 記録 → 設定 → 手書きを消す → GIGA山
提示モードに Enter で入れる／戻れる    ✅
モーダル内にフォーカスが閉じ込められる  ✅
Esc で閉じ、元のボタンにフォーカスが戻る ✅（「設定をひらく」へ戻った）
```

---

## 監査表

### A. 法務・配布

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| A1 | LICENSE 実ファイル | ✅ | MIT / Copyright (c) 2026 GIGAyama |
| A2 | .gitignore | ✅ | `node_modules/` `dist/` `*.local` `.DS_Store` |
| A3 | dependabot.yml | ✅ | npm / github-actions ともに monthly、メジャー更新は対象外 |
| A4 | README / MANUAL / AUDIT | ✅ | 3点そろえた（この回で提示モード・印刷の節を追記） |

### B. セキュリティ

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| B1 | CSP（入れたうえで動作確認済み） | ✅ | 全画面で**違反0件・JS エラー0件**。`script-src 'self'`（`'unsafe-inline'` 無し）。`style-src` の `'unsafe-inline'` は React が style 属性でマスの大きさを渡すため外せない |
| B2 | 秘密情報・IDの直書きなし | ✅ | `.env` / `.clasp.json` はコミットされていない |
| B3 | OAuthスコープ最小 | — | 該当なし（GAS を使っていない） |
| B4 | postMessage の宛先が `*` でない | ✅ | Service Worker への `{type:'SKIP_WAITING'}` のみ |
| B5 | サーバー側5段ガード | — | 該当なし（サーバーを持たない・個人情報を扱わない） |
| B6 | **CDN から取る実行コードが 0** | ✅ | 0件（第1回で TensorFlow.js を自己ホスト化済み） |
| B7 | 残る外部資産に SRI と版の固定 | — | 実行コードの外部依存が 0。残るのは Google Fonts のみで「見た目だけ」の依存。CSS の中身が端末ごとに変わるためハッシュは固定できない |
| B8 | `npm audit` | ⚠️ | **2件**（moderate 1 / high 1）。いずれも `esbuild`（開発サーバーのみ・ブラウザへは届かない）。解消には vite 4→8 のメジャー更新が要る。**授業を止めるリスクがあるため行っていない**（§絶対安全規則 6） |

### C. 堅牢性

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| C1 | LockService + try/finally | — | 該当なし |
| C2 | 自動復旧 | ✅ | 学習ログが壊れていても空からやり直す |
| C3 | pagehide で記録確定 | ✅ | Chromebook のタブ破棄対策 |
| C4 | 通信失敗時のリトライと明示 | ✅ | 手書きモデルの読み込みに失敗したら画面に明示し、設定で入れ直せば再試行する |
| C5 | localStorage.clear() を使っていない | ✅ | 0件 |

### D. 表示（Part I §2）

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| D1 | viewport に viewport-fit=cover | ✅ | あり。`interactive-widget=resizes-content` も維持 |
| D2 | 100dvh を使用 | ✅ | `@supports not` でフォールバック |
| D3 | safe-area-inset を適用 | ✅ | `body` の左右と下部の固定要素 |
| D4 | clamp() による fluid type | ✅ | `--fs-body` / `--fs-hero` ほか |
| D5 | Canvas に DPR 補正（上限2） | ✅ | 実測 dpr 3 → 2倍で頭打ち |
| D6 | 320px 幅で横スクロールが出ない | ✅ | **320px〜3840px の7サイズで 0件**（提示モードを含む） |
| D7 | 画像に width/height、150KB以下 | ✅ | `<img>` は使っていない。PNG は最大 42.3 KB |
| D8 | **コントラスト 4.5:1 以上（実ブラウザで全画面走査）** | ✅ | **0件**（6画面×4サイズ） |
| D9 | **タップ領域 44px 以上（`::after` 込みで実測）** | ⚠️ | 計算マス以外は **0件**。計算マスのみ 320px・375px で未達（後述） |
| D10 | prefers-reduced-motion（`.01ms` であって 0 でない） | ✅ | `.01ms` |
| D11 | forced-colors 対応 | ✅ | **実測した。片方だけ受けていて壊れていたのを直した**（1/3 → 3/3 見分けられる） |
| D12 | 提示モード | ✅ | **実装した。3840px でマス 104px・数字 52px** |
| D13 | 印刷CSS | ✅ | **実測した。表が切れていたのと、手書きパネルが紙に出ていたのを直した** |
| D14 | 拡大を禁止していない | ✅ | `user-scalable=no` / `maximum-scale` は無し |

### E. PWA（Part I §3）

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| E1 | manifest の id/scope/start_url がリポジトリ名絶対パス | ✅ | `/online-100square-calculation/`。**今回も変えていない** |
| E2 | アイコン4種 + 透明を含まない apple-touch-icon | ✅ | 透明画素なし（PNG のチャンクを読んで確認） |
| E3 | beforeinstallprompt を head 最上部で捕捉（外部ファイル） | ✅ | `install-hook.js` |
| E4 | インストールボタン（案内できるときだけ表示） | ✅ | 「アプリに追加」 |
| E5 | sw.js が自アプリ接頭辞のキャッシュのみ削除 | ✅ | **実測で確認**（他アプリのキャッシュ2つが残った） |
| E6 | sw.js が localStorage に触れていない | ✅ | 0件 |
| E7 | 更新通知（押すまで切り替わらないことを実測） | ✅ | 版を上げて3秒放置 → `waiting` のまま・画面遷移 0回 |
| E8 | **初回訪問で勝手にリロードしない** | ✅ | 画面遷移 **1回** |
| E9 | **Service Worker が実際に登録されている** | ✅ | `getRegistration()` で `activated` |
| E10 | offline.html（外部資産・JS に頼らない） | ✅ | 実測で表示を確認 |
| E11 | SW の版が配信物の中身から自動で作られる | ✅ | `tools/build-sw.mjs` が `dist/sw.js` に刻む。手書きに戻すと `SW_APP_VERSION` が止める（2026-08-22 に自動化。それまでは手書きで、上げ忘れると直した画面が端末に届かなかった） |
| E12 | 学習記録に刻む版が `package.json` と揃っている | ✅ | `STUDY_APP_VERSION` が見る。2026-08-22 まで見ておらず、`src/studySession.js` は 1.6.0 のまま `package.json` は 1.7.1 だった |
| E12 | maskable のセーフゾーン外の中身 0.2% 以下 | ✅ | 0.027%（第1回の実測値。アイコンは今回変更していない） |
| E13 | iOS の「ホーム画面に追加」手順を MANUAL に記載 | ✅ | MANUAL.md §4 |

### F. アクセシビリティ・性能

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| F1 | alt / aria-label / aria-live / role="alert" | ✅ | 提示モードのボタンにも `aria-label` と `aria-pressed` を付けた |
| F2 | モーダルに role="dialog"・Esc で閉じる | ✅ | フォーカス閉じ込めと復帰を実測 |
| F3 | キーボードのみで全機能に到達 | ✅ | 提示モードを含めて実測 |
| F4 | rt の色を決め打ちしていない | ✅ | 色のついた面ではまとめて継がせる |
| F5 | 初回JS 300KB以下 | ✅ | **208.9 KB**（gzip 64.8 KB） |
| F6 | 1ファイル 5,000行 / 400KB 以内 | ✅ | 最大は `App.jsx` の 1,772行 / 86.8KB |

### G. 学習ログ

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| G1 | study.v1 準拠・個人情報を持たない | ✅ | 今回は触っていない |
| G2 | 中断記録・5分ルール | ✅ | 同上 |

---

## 直していないもの（理由つき）

### D9 計算マスのタップ領域（320px・375px のときのみ）

第1回と同じ判断である。10列の表を **320px 幅で横スクロールなしに収める**（D6）ことと、
**1マスを 44px 以上にする**（D9）ことは同時に成り立たない。
44px×10列には最低 466px 必要で、320px の画面には物理的に入らない。

D6 を優先した。100マス計算では**全体が一度に見えること自体が課題の一部**だからである。
そのうえで、マスを直接押さなくても解けるようにしてある（手書き・テンキーで自動送り、矢印キー＋Enter）。
810px 以上（iPad・Chromebook・電子黒板）では 0件。実測値：320px で 23.8×27.2px、375px で 28.8×27.2px。

### B8 esbuild / vite の脆弱性

開発サーバーだけの問題で、ビルド成果物（＝児童のブラウザへ届くもの）には影響しない。
解消には vite 4 → 8 のメジャー更新が必要で、`@vitejs/plugin-react` や
`vite-plugin-pwa` の更新も連鎖する。**授業を止めるリスクのほうが大きい**と判断した。

---

## 測っていないもの

**測っていないものを ✅ とは書かない。** 以下は未計測である。

- **実機での確認。** Chromebook・iPad・電子黒板の実物では測っていない。すべて headless の Chromium 上での実測である。
  提示モードは 3840×2160 の viewport で測ったが、**65〜75インチの実物で「うしろの席から読めるか」は測っていない**
- **本番（`gigayama.github.io`）での確認。** 作業環境からプロキシで到達できない（403）
- **実際の紙。** A4 縦の印刷可能幅で組み直して寸法を測り、`page.pdf()` も出したが、
  **プリンタから出てきた紙そのものは見ていない**。プリンタ側の「用紙に合わせる」設定は結果を変える
- **Windows の実物のハイコントラストモード。** Chromium の `forcedColors: 'active'` での実測である。
  Windows のハイコントラストテーマは配色が複数あり、`Highlight` / `CanvasText` の実際の色は選んだテーマによる
- **iOS Safari の全画面。** iPad の Safari は `requestFullscreen` に対応していないため、
  提示モードでは「文字とマスを大きくする」ところまでしか効かない。**これは実機で確かめていない**
- **iOS Safari の挙動全般。** ITP による保存データの消え方、ホーム画面でのアイコンの見え方
- **LCP / CLS の実数値。** アセット量は測ったが、実機での表示速度そのものは測っていない
- **手書き認識の精度。** 0〜9 の認識率は測っていない（第1回から変わらず）
- **オフラインでの手書き入力（初回）。** TensorFlow.js は先読みに入れていないため、
  一度もオンラインで手書きを使っていない端末では、圏外だと手書きが使えない

---

## 人間に判断してほしいこと

1. **提示モードを入れた。** 第1回で「先生の使い方を確認したうえで判断したい」と書いて保留にしていたものを、
   §2-11 の「一斉授業で使うアプリには必ず用意する」に従って実装した。
   足しただけで、使わなければこれまでどおりである。**要らなければ外せる**（`quality.config.json` の
   `presentationMode` を `false` にすると品質ゲートも黙る）。
2. **正誤の見た目を変えた。** 正解＝実線の枠／まちがい＝破線の枠を、ハイコントラストだけでなく
   **ふつうの表示にも足した**。色だけで意味を伝えないための変更だが（§2-8）、見た目は変わる。
   文言・機能・配色そのものは変えていない。
3. **B8 の `npm audit` 2件。** vite のメジャー更新をいつ行うか。

> `manifest` の `id` / `scope` / `start_url` は**変更していない**ため、
> すでに端末に入っているアプリが別扱いになることはない。

---

## 他のリポジトリにも効きそうなこと

- **`@media print` を書いてあるだけでは足りない。** 画面用の `overflow-x: auto`
  （Tailwind なら `overflow-x-auto`）が残っていると、紙でははみ出しがそのまま切り取られる。
  **紙の幅（A4 縦なら 703px）で組み直して測らないと見えない。**
  `grep -l "overflow-x-auto" $(git ls-files) | xargs grep -L "@media print"` で候補が出る
- **`@media print` に `.no-print` を書いても、あとから足したパネルには付いていない。**
  紙に出るものは「消し忘れ」ではなく「足したときに付け忘れ」で増える
- **forced-colors の受けは、必ず対で書く。** 正解にだけ枠を足すと、まちがいが未回答と同じ見た目になる。
  ハイコントラストでは面の色が両方とも消えるためで、**片方だけ見ていると気づけない**
- **提示モードは「文字を大きくする」だけでは効かない。** 本文幅の上限（`max-w-6xl` など）を
  外さないと、4K に映しても中身は 1152px のままである
- **`--fs-hero` を画面幅だけで決めると、幅の決まった列の中では破綻する。**
  v5 §2-11 のひな形をそのまま当てて 372px はみ出した。**列を広げてから字を大きくする**
- **品質ゲートは「わざと壊して」確かめる。** 今回も、足した検査そのものの不具合が2件出た。
  とくに「別々のファイルにある条件を1ファイルずつ見ていて、永久に一致しない」形は、
  **成功しているのと区別が付かない**

---

## 検査の走らせかた

```bash
npm ci
npm run build      # 先読みキャッシュの量を見る検査があるので先にビルドする
npm run check              # 品質ゲート（19項目）
npm run check -- --self-test   # 検査そのものが動いているかを確かめる
```

CI（`.github/workflows/ci.yml`）は **`pull_request` と `push` の両方**で動く。
