# AUDIT — 100マス計算（GIGA Standard v5）

- 対象: `GIGAyama/online-100square-calculation`
- 型: **B型**（Vite + React / GitHub Pages）
- 監査日: 2026-08-04
- 版: `1.3.1` → **`1.4.0`**

## 測りかた（推測で書かないための前提）

- **実ブラウザ（Chromium）で開いて測った。** 静的に読んだだけの項目は「未計測」と書く。
- コントラストは §7-2 のとおり、色を **1px 実際に塗って `getImageData` で読む**方法で測った。
  数値を文字列から拾う方法は、グラデーション背景・`currentColor`・`oklch()` で壊れる。
- 測った画面は **初期 / プレイ中 / 設定 / 記録 / 中断かくにん** の5つ ×
  **320×568・375×667・810×1080・1366×768** の4サイズ。
  記録画面は空のままだと一度も出ない色があるため、**記録が入った状態も作って測った**。
- 「改修前」の数字は、**改修前のコードを実際にビルドして同じ手順で測った値**である
  （比較対象を測らずに「壊れていた」とは書かない §7-6）。
- 作業環境からは `fonts.googleapis.com` と `cdn.jsdelivr.net` へ出られない。
  これは**学校のフィルタリングと同じ状態**なので、そのまま利点として使った。

---

## いちばん大きかった問題

**手書き入力が、学校のネットワークでは動かなかった。**

TensorFlow.js を `cdn.jsdelivr.net` から `<script>` で読み込んでいた。
CDN が塞がれた状態で実際に開くと、こうなる。

```
https://cdn.jsdelivr.net/npm/@tensorflow/tfjs@3.21.0 :: net::ERR_TUNNEL_CONNECTION_FAILED
window.tf = undefined
画面の表示: 「AIの準備に失敗しました」
```

手書きは**既定でオン**なので、多くの児童が影響を受ける。
しかも原因がアプリの外（学校の設定）にあるため、先生が調べても分からない。

→ npm の同じ版を**自分側に置き**、`import()` で手書きを使うときだけ読み込む形にした。
CDN が塞がれたままの環境で、**モデルの読み込みと推論が通ることを実測した**。

| | 改修前 | 改修後 |
|---|---|---|
| 手書きモデルの読み込み | 「AIの準備に失敗しました」 | **「手書き入力が使えます」** |
| 実際に書いて推論 | 走らない | **「「1」を入力しました」** |
| CDN から取る実行コード | 1件 | **0件** |

---

## 実測（改修前 → 改修後）

| 項目 | 前 | 後 |
|---|---:|---:|
| **コントラスト基準未満**（4サイズ×5画面 のべ） | **199件** | **0件** |
| タップ44px未満（のべ） | 108件 | 8件（※） |
| CSP 違反 | ―（CSP 無し） | **0件** |
| JS エラー（Google Fonts の遮断を除く） | **2件**（TF.js の失敗） | **0件** |
| 320px 幅での横スクロール | 0件 | 0件 |
| 先読みキャッシュ | 1440.6 KB | **798.3 KB** |
| 初回表示に必要な JS | 197.2 KB (gzip 62.4) | 206.9 KB (gzip **63.9**) |
| アイコン画像 6点 合計 | 766.0 KB | **144.3 KB** |
| maskable セーフゾーン外の中身 | 0.545% | **0.027%** |

※ 残り8件はすべて**計算マスそのもの**で、320px・375px 幅のときだけ出る。後述。

### コントラストの内訳（改修前に基準を割っていたもの）

| 文言 | 色 | 比 | 直しかた |
|---|---|---:|---|
| 「?」（答えを書く場所のしるし） | `blue-300` | **1.66** | `blue-500` |
| 「まだ記録がありません」 | `slate-400` | **2.45** | `slate-500` |
| 「最近の記録（20回）」 | `slate-400` | **2.56** | `slate-500` |
| 「とちゅうでやめた回数」 | `slate-400` on `blue-50` | **2.56** | `slate-600` |
| 「のこり n問」 | `slate-500` on `blue-50` | **4.37** | `slate-600` |
| 設定の説明文（押している最中） | `slate-500` on `slate-100` | **4.34** | `slate-600` |
| ふりがな（「閉じる」ボタンの上） | `rt { color:#64748b }` on `slate-700` | **2.18** | 継がせる |
| ふりがな（「消す」ボタンの上） | 同上 on `slate-100` | **4.34** | 継がせる |

**いちばん読みにくかったのは、いちばん困っている人に向けた文だった。**
「?」は「ここに答えを書く」という合図であり、「まだ記録がありません」は
まだ一度も取り組めていない児童にだけ出る文である。

**ふりがなは1か所ずつ潰さない。** `rt` の色を決め打ちしていたため、色のついた面の上で
すべて読めなくなっていた。ふりがなが要るのは低学年の児童なので、
**いちばん読めなくて困る人が、いちばん読めない**形になっていた。
色のついた面ではまとめて下の文字の色を継がせる形にした。

### 画像（パレット PNG 化）

| ファイル | 前 | 後 |
|---|---:|---:|
| `favicon.png`（512→256 に縮小） | 279.6 KB | **3.2 KB** |
| `pwa-512x512.png` | 259.1 KB | **42.3 KB** |
| `pwa-maskable-512x512.png` | 152.1 KB | **36.4 KB** |
| `pwa-192x192.png` | 42.0 KB | **12.3 KB** |
| `apple-touch-icon.png` | 33.2 KB | **10.3 KB** |
| `pwa-maskable-192x192.png` | （無かった） | **5.5 KB** |
| **合計** | **766.0 KB** | **144.3 KB** |

maskable は「欠け」ではなく**余白の使い方**が問題だった。下地はもともと端まで
伸びていたが、絵と長い影がセーフゾーン（中央80%の円）からはみ出していた。
下地はそのままに、**絵だけを 0.64 倍にして中へ収めた**。

なお、画素を数えるときは**下地と「欠けては困る中身」を色で区別した**。
下地込みで数えると 1.953%、青い絵だけで数えると 0.545% で、実態は後者である。

### PWA の挙動（§7-5）

| 確かめたこと | 結果 |
|---|---|
| Service Worker が実際に登録されている | ✅ `active` |
| **初回訪問で勝手にリロードしない** | ✅ 画面遷移 **1回** |
| **押すまで切り替わらない**（版を上げて3秒放置） | ✅ `waiting` のまま・画面遷移 0回 |
| 押したら切り替わる | ✅ 再読み込み1回、`square100-static-v1.4.0` が消え `v1.4.1` になった |
| **他アプリのキャッシュを巻き添えにしない** | ✅ `townmap-mikke-static-v2` / `keisan-card-runtime-v1` は残った |
| 圏外で起動する | ✅ |
| 本体の控えを消すと `offline.html` が出る | ✅ 「いま インターネットに つながっていません」 |
| TensorFlow.js が先読みに入っていない | ✅ |
| Canvas の DPR 補正（上限2） | ✅ dpr 1→1倍 / 2→2倍 / **3→2倍で頭打ち** |

### キーボードだけで全機能に到達できるか

```
Tab 順: 計算の種類 → 問題数 → スタート！ → 記録 → 設定 → 手書きを消す → GIGA山
キーボードだけでスタート          ✅
数字キーで解答 → 次のマスへ自動で進む ✅
モーダル内にフォーカスが閉じ込められる ✅
Esc で閉じ、元のボタンにフォーカスが戻る ✅
```

---

## 監査表

### A. 法務・配布

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| A1 | LICENSE 実ファイル | ✅ | MIT / Copyright (c) 2026 GIGAyama |
| A2 | .gitignore | ✅ | `node_modules/` `dist/` `*.local` `.DS_Store` |
| A3 | dependabot.yml | ✅ | npm / github-actions ともに monthly、メジャー更新は対象外 |
| A4 | README / MANUAL / AUDIT | ✅ | 3点そろえた |

### B. セキュリティ

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| B1 | CSP（入れたうえで動作確認済み） | ✅ | 全画面で**違反0件・JS エラー0件**。`script-src 'self'`（`'unsafe-inline'` 無し）。`style-src` の `'unsafe-inline'` は React が style 属性でマスの大きさを渡すため外せない |
| B2 | 秘密情報・IDの直書きなし | ✅ | `.env` / `.clasp.json` はコミットされていない |
| B3 | OAuthスコープ最小 | — | 該当なし（GAS を使っていない） |
| B4 | postMessage の宛先が `*` でない | ✅ | `postMessage` は Service Worker への `{type:'SKIP_WAITING'}` のみ |
| B5 | サーバー側5段ガード | — | 該当なし（サーバーを持たない・個人情報を扱わない） |
| B6 | **CDN から取る実行コードが 0** | ✅ | **1件 → 0件**（TensorFlow.js を自己ホスト化） |
| B7 | 残る外部資産に SRI と版の固定 | — | 実行コードの外部依存が 0 になったため該当なし。残る外部資産は Google Fonts だけで、これは「見た目だけ」の依存（塞がれても端末側フォントで動く）。Google Fonts の CSS は端末ごとに中身が変わるためハッシュを固定できない |
| B8 | `npm audit` | ⚠️ | `npm audit fix`（`--force` なし）で 5件 → **2件**。残りは `esbuild`（開発サーバーのみ・ブラウザへは届かない）で、解消には vite 4→8 のメジャー更新が要る。**授業を止めるリスクがあるため行っていない**（§絶対安全規則 6） |

### C. 堅牢性

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| C1 | LockService + try/finally | — | 該当なし |
| C2 | 自動復旧 | ✅ | 学習ログが壊れていても空からやり直す（`studyLog.js`） |
| C3 | pagehide で記録確定 | ✅ | 改修前から実装済み。Chromebook のタブ破棄対策 |
| C4 | 通信失敗時のリトライと明示 | ✅ | 手書きモデルの読み込みに失敗したら画面に明示し、**設定で入れ直せば再試行する**ようにした |
| C5 | localStorage.clear() を使っていない | ✅ | 0件。`study.records.v1` は他アプリ共通のため触らない |

### D. 表示（Part I §2）

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| D1 | viewport に viewport-fit=cover | ✅ | あり。`interactive-widget=resizes-content` も維持 |
| D2 | 100dvh を使用 | ✅ | `.app-shell` / `.modal-shell` を dvh に。`@supports not` でフォールバック |
| D3 | safe-area-inset を適用 | ✅ | `body` の左右パディングと、下部の固定要素（更新のおしらせ・フッター） |
| D4 | clamp() による fluid type | ✅ | `--fs-body` / `--fs-hero` ほか。いま解いている計算式を「主役」として `clamp(26px, 4.5vw+12px, 48px)` に |
| D5 | Canvas に DPR 補正（上限2） | ✅ | **実測 dpr 3 → 2倍で頭打ち**。以前は 320px 固定で、横長の枠では絵が歪んで推論に渡っていた |
| D6 | 320px 幅で横スクロールが出ない | ✅ | 0件 |
| D7 | 画像に width/height、150KB以下 | ✅ | `<img>` は使っていない（アイコンは lucide-react の SVG）。PNG は最大 42.3 KB |
| D8 | **コントラスト 4.5:1 以上（実ブラウザで全画面走査）** | ✅ | **199件 → 0件** |
| D9 | **タップ領域 44px 以上（`::after` 込みで実測）** | ⚠️ | 108件 → **8件**。残りは計算マスのみ（後述の「直していないもの」） |
| D10 | prefers-reduced-motion（`.01ms` であって 0 でない） | ✅ | `.01ms`。0 にすると `fill-mode: forwards` が壊れて要素が消える |
| D11 | forced-colors 対応 | ⚠️ | CSS は入れた（ボタン・マスの輪郭、正解マスの枠）。**実際の Windows ハイコントラストでの見え方は未計測** |
| D12 | 提示モード | ❌ | **未実装**（後述） |
| D13 | 印刷CSS | ⚠️ | 実装した（A4縦・表だけ・16mm角）。**印刷プレビューの実物は未計測**。手順は MANUAL.md §7 |
| D14 | 拡大を禁止していない | ✅ | `user-scalable=no` / `maximum-scale` は無し |

### E. PWA（Part I §3）

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| E1 | manifest の id/scope/start_url がリポジトリ名絶対パス | ✅ | 改修前から `/online-100square-calculation/`。今回は変えていない |
| E2 | アイコン4種 + 透明を含まない apple-touch-icon | ✅ | maskable-192 を追加して4種そろえた。`apple-touch-icon.png` は**透明画素なし**（PNG のチャンクを読んで確認） |
| E3 | beforeinstallprompt を head 最上部で捕捉（外部ファイル） | ✅ | `install-hook.js` を読み込みを伴うタグの中でいちばん先に置いた |
| E4 | インストールボタン（案内できるときだけ表示） | ✅ | 「アプリに追加」。すでに入れてある／案内できないときは出さない |
| E5 | sw.js が自アプリ接頭辞のキャッシュのみ削除 | ✅ | **実測で確認**（他アプリのキャッシュ2つが残った） |
| E6 | sw.js が localStorage に触れていない | ✅ | 0件 |
| E7 | 更新通知（押すまで切り替わらないことを実測） | ✅ | 版を上げて3秒放置 → `waiting` のまま |
| E8 | **初回訪問で勝手にリロードしない** | ✅ | 画面遷移 **1回** |
| E9 | **Service Worker が実際に登録されている** | ✅ | `getRegistration()` で `active` |
| E10 | offline.html（外部資産・JS に頼らない） | ✅ | 実測で表示を確認。CSS も文字も自前、再読み込みは `<a href>` |
| E11 | APP_VERSION を今回のリリース値に更新した | ✅ | `v1.4.0`。`package.json` の version との食い違いは品質ゲートが見る |
| E12 | maskable のセーフゾーン外の中身 0.2% 以下 | ✅ | **0.027%**（192 は 0.090%） |
| E13 | iOS の「ホーム画面に追加」手順を MANUAL に記載 | ✅ | MANUAL.md §4 |

### F. アクセシビリティ・性能

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| F1 | alt / aria-label / aria-live / role="alert" | ✅ | アイコンのみのボタンに `aria-label`、中のアイコンに `aria-hidden`。結果・手書きの状態・更新のおしらせを `role="status" aria-live="polite"` に |
| F2 | モーダルに role="dialog"・Esc で閉じる | ✅ | 3つのモーダルすべてに `role="dialog" aria-modal="true"`。**フォーカス閉じ込めと復帰を実測** |
| F3 | キーボードのみで全機能に到達 | ✅ | 実測（上記） |
| F4 | rt の色を決め打ちしていない | ✅ | 色のついた面ではまとめて継がせる。`<rp>` も85か所すべてに添えた |
| F5 | 初回JS 300KB以下 | ✅ | **206.9 KB**（gzip 63.9 KB）。TensorFlow.js は別チャンクで、手書きを使うときだけ読む |
| F6 | 1ファイル 5,000行 / 400KB 以内 | ✅ | 最大は `App.jsx` の 1,705行 / 82.7KB |

### G. 学習ログ

| # | 項目 | 判定 | 実測 |
|---|---|:--:|---|
| G1 | study.v1 準拠・個人情報を持たない | ✅ | 改修前から準拠。今回は触っていない |
| G2 | 中断記録・5分ルール | ✅ | 同上（`pagehide` での確定も含む） |

---

## 直していないもの（理由つき）

### D9 計算マスのタップ領域（320px・375px のときのみ）

10列の表を **320px 幅で横スクロールなしに収める**（D6）ことと、
**1マスを 44px 以上にする**（D9）ことは同時に成り立たない。
44px×10列には最低 466px 必要で、320px の画面には物理的に入らない。

D6 を優先した。理由は、100マス計算では**全体が一度に見えること自体が課題の一部**だからである。
そのうえで、マスを直接押さなくても解けるようにしてある。

- 手書き・テンキー入力では、正解すると**自動で次のマスへ進む**
- 物理キーボードの矢印キーと Enter でも移動できる
- 810px 以上（iPad・Chromebook・電子黒板）では **0件**

実測値：320px で 23.8×27.2px、375px で 28.8×27.2px。

### D12 提示モード

このアプリは各自の端末で個別に取り組む使い方が中心で、電子黒板に映して
一斉に見る場面が想定しにくいため、今回は入れていない。
入れる場合は、`--fs-hero` と `.presentation` を使えば小さく足せる。
**先生の使い方を確認したうえで判断したい。**

### B8 esbuild / vite の脆弱性

開発サーバーだけの問題で、ビルド成果物（＝児童のブラウザへ届くもの）には影響しない。
解消には vite 4 → 8 のメジャー更新が必要で、`@vitejs/plugin-react` や
`vite-plugin-pwa` の更新も連鎖する。**授業を止めるリスクのほうが大きい**と判断した。

---

## 測っていないもの

**測っていないものを ✅ とは書かない。** 以下は未計測である。

- **実機での確認。** Chromebook・iPad の実物では測っていない。すべて headless の Chromium 上での実測である
- **本番（`gigayama.github.io`）での確認。** 作業環境からプロキシで到達できない（403）。
  計測はすべてローカルで `dist/` を配信して行った
- **iOS Safari の挙動。** `apple-touch-icon` に透明画素が無いことは PNG のチャンクを読んで確認したが、
  実際のホーム画面での見え方、ITP による保存データの消え方は未計測
- **LCP / CLS の実数値。** アセット量は測ったが、実機での表示速度そのものは測っていない
- **Windows のハイコントラストモードでの見え方。** CSS は入れたが、実際の表示は未計測
- **印刷の実物。** `@media print` は書いたが、プレビュー・実際の紙は未計測
- **手書き認識の精度。** DPR 補正で canvas の解像度が変わったため、線の太さを
  解像度に対する割合で決め直した。「1」が認識されることは実測したが、
  **0〜9 の認識率が改修前と同じかどうかは測っていない。** 実機で確かめてほしい
- **オフラインでの手書き入力（初回）。** TensorFlow.js は先読みに入れていないため、
  一度もオンラインで手書きを使っていない端末では、圏外だと手書きが使えない。
  一度使えば以降は圏外でも使える（実行時キャッシュ）

---

## 人間に判断してほしいこと

1. **ビルド構成を変えた。** Service Worker を `vite-plugin-pwa` の自動生成から
   自前の `src/sw.js`（`injectManifest`）へ移した。`registerType: 'autoUpdate'` のままでは
   「押すまで切り替えない」を満たせないためだが、アーキテクチャの変更にあたるので
   **マージの判断は人間に委ねる**（§停止条件）。
2. **提示モードを入れるかどうか。** 一斉授業で電子黒板に映す使い方があるなら追加する。
3. **オフラインでの手書き。** 先読みに 1.4MB を足せば初回から圏外でも手書きが使えるが、
   校内 Wi-Fi で40人が同時に開いたときの初回表示が確実に遅くなる。今は「使った端末だけが
   取り込む」形にしている。**どちらを取るか。**

> `manifest` の `id` / `scope` / `start_url` は**変更していない**ため、
> すでに端末に入っているアプリが別扱いになることはない。

---

## 他のリポジトリにも効きそうなこと

- **`rt` の色の決め打ちはここにもあった。** `rt { color: #64748b }` で、
  濃いボタンの上で比 2.18。§4 の指摘どおり、3本調べて3本ともという形の穴である
- **`registerType: 'autoUpdate'`** を使っている `vite-plugin-pwa` のリポジトリは、
  すべて「児童の操作中に勝手に切り替わる」状態になっている。設定1行では直らず、
  自前の `sw.js` が要る
- **TensorFlow.js / pdf.js のような「重い実行コード」を CDN から動的に `<script>` で
  差し込んでいる**箇所は、`@babel/standalone` と同じ理由で壊れる。
  `grep -rn "createElement('script')" src/` で見つかる
- **アイコンのパレット PNG 化は、どのリポジトリでも効く。** ここでは 766KB → 144KB
- **品質ゲートは「わざと壊して」確かめると、検査自体の不具合が出る。**
  今回も2件出た（`VIEWPORT_100VH` が `@supports` より後ろの `100vh` を見落とす、
  `INSTALL_HOOK` がスタイルシートより後ろに置かれても通してしまう）

---

## 検査の走らせかた

```bash
npm ci
npm run build      # 先読みキャッシュの量を見る検査があるので先にビルドする
npm run check              # 品質ゲート
npm run check -- --self-test   # 検査そのものが動いているかを確かめる
```

CI（`.github/workflows/ci.yml`）は **`pull_request` と `push` の両方**で動く。
