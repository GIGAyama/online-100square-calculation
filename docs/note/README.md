# note 入稿メモ（100マス計算の紹介記事）

このフォルダの中身。

```
docs/note/
├── square100-note-article.md   記事本文。そのまま note に貼れる形
├── images/                     実際にアプリを操作して撮った画面 19点
└── README.md                   このファイル
```

本文は約 9,900 字です。装飾記号を使わず、Markdown は見出しだけで書いてあります。

## 貼るまえに直すところ

- **連載番号**。1行目のタイトルが `#◯` のままです。実際の番号に置きかえてください。推測で埋めると連載が壊れるので、あえて空けてあります。
- タイトルはこの案で置いてあります。差しかえてかまいません。

  `教室で使えるかもしれないもの作り #◯ 「40人ぶんの丸つけ、いつやりますか」書いた瞬間に採点される100マス計算アプリ「100マス計算」`

- 記事の最後に「使ってみて『ここが違う』と思われたら、ぜひ教えてください」と書いてあります。連絡先を出したくない場合はここを削ってください。

## note に貼る手順

1. note の新規記事を開き、1行目にタイトルを入れる。本文の `# ` から始まる行がタイトルです。この行は本文には貼らない
2. `## ` から始まる見出しは、note のエディタで「見出し」として入れ直す。note は見出しから目次を自動で作るので、ここが記事の骨格になります
3. 本文をそのまま貼る。太字も表も使っていないので、貼り付けで崩れるところはありません
4. 画像は自動で入りません。`images/` の中のファイルを、本文の `![...](images/xx-....png)` が置いてある位置に手でアップロードする
5. 画像の下に付いている一行の説明は、note のキャプション欄に移す。本文からは消す
6. `https://online-100square-calculation.giga-school.com/` の行は、note に貼ると自動でリンクカードになります。カードにしたくなければテキストリンクに変えてください
7. ハッシュタグ欄の案。`#GIGAスクール` `#小学校` `#算数` `#100マス計算` `#ICT教育` `#教育アプリ`

## 画像の一覧

上から本文に出てくる順です。すべて実機のブラウザで操作して撮ったもので、加工も合成もしていません。

| ファイル | 何の画面か | 本文のどこで使うか |
|---|---|---|
| `01-home.png` | ホーム画面（スマートフォン縦） | 📱 このアプリでできること の冒頭 |
| `02-desktop-home.png` | Chromebook の横長画面 | 同上 |
| `03-playing.png` | 開始直後。式と残り問題数が出ている | 同上 |
| `04-progress.png` | 途中まで解いたところ。正解が緑 | 同上 |
| `05-hikizan.png` | 引き算の盤面 | 同上 |
| `06-kakezan.png` | かけ算の盤面 | 同上 |
| `07-result.png` | 100問クリアの結果画面 | 同上 |
| `08-marking.png` | すぐ判定を切って、あとから丸つけ | 同上 |
| `09-quit-confirm.png` | 「とちゅうで やめますか？」 | 同上 |
| `10-handwriting.png` | 手書き欄に「7」を書いた瞬間 | ✍️ 指で数字を書いて答える |
| `11-handwriting-done.png` | 読み取られてマスに入ったところ | 同上 |
| `12-desktop-playing.png` | Chromebook で解いているところ | 同上 |
| `13-numpad.png` | ボタン入力（テンキー） | 同上 |
| `14-settings.png` | 設定モーダル | 同上 |
| `15-input-left.png` | 入力ツールを左に置いた状態 | 同上 |
| `16-stats.png` | 記録画面の上（たし算） | 📊 タイムより「一発で正解できた率」 |
| `17-stats-modes.png` | 記録画面の下（引き算・かけ算） | 同上 |
| `18-presentation.png` | 提示モード（1920×1080） | ✨ 導入のメリット |
| `19-print.png` | 印刷したときの見え方 | 同上 |

画像はすべて実サイズの2倍で撮ってあるので、note で拡大しても粗くなりません。

## 記事に書いた数字の出どころ

| 記事の記述 | 出どころ |
|---|---|
| 100問を3分23秒 | 撮影時に実際に100マスを解いた結果。`07-result.png` に 203.7 秒と出ている |
| 手書きで「7」と読み取られた | 撮影時の実行ログ。画面にも「「7」を入力しました」と出ている（`11-handwriting-done.png`） |
| 9×8 と 7×8 を外していた | `17-stats-modes.png` の「よく間違えた計算」。撮影で実際に誤答したもの |
| 問題数は10問から100問まで10問きざみ | `src/App.jsx` の問題数の選択肢。10 段階 |
| 計算は3種類 | `src/App.jsx` の計算の種類の選択肢 |
| 上の行は0から9が1回ずつ並べ替えられる | `src/App.jsx` の `generateTable`。列は `shuffle([0..9])`、行は毎回ランダムに選び直す |
| 引き算は10いくつから1桁を引く | `src/App.jsx` の `generateTable`。引き算のときだけ行の値が 10〜19 になる |
| 最初に読み込むのは22ファイル・約800キロバイト | `npm run build` の出力。`precache 22 entries (801.42 KiB)` |
| マスは1辺16ミリで印刷される | `src/index.css` の `@media print`。`width: 16mm` |
| A4 縦1ページに収まる | `AUDIT.md`。印刷可能幅 703px に対して表の右端 685.1px |
| 65〜75インチの電子黒板で読める | `AUDIT.md` の提示モードの実測。3840×2160 でマス 104px・数字 52px |
| 記録は直近20回 | `src/App.jsx` の `history` を 20 件で切っている |
| タブを離れて5分で中断として記録 | `src/App.jsx` の中断タイマー。`5 * 60 * 1000` |
| 1分操作がないと勉強時間を数えない | `src/App.jsx` の `useActiveTimeTracker`。60 秒 |
| 許可が要るのは online-100square-calculation.giga-school.com | `public/CNAME` と `vite.config.js` の公開先。`index.html` の CSP は `default-src 'self'` |
| つなぎ先は本体のほかに2つ（書体と記録の受け渡し） | `index.html` の CSP。`font-src` に fonts.gstatic.com、`frame-src` に https://gamification.giga-school.com |
| まなびクエストへ記録を写す | `index.html` が読み込む `public/records-hub-client.js`。見えない iframe と postMessage で、同じブラウザの中の記録ハブへ写す。送り先は固定で、サーバーへの送信はない |
| 学年の設定がない／計算は3種類でわり算はない | `src/App.jsx` の選択肢。たし算・引き算・かけ算だけで、学年を選ぶ項目がない |
| 順位表もランキングもない | `src/studyStats.js`。集計はその端末の `study.records.v1` だけを読む。他者の記録を受け取る経路がない |
| MIT ライセンス | `LICENSE` |
| 記録は端末の中だけ、学校の外のサーバーへ送らない | `src/studyLog.js`。`localStorage` への保存だけで、ネットワークへ送る処理がない（まなびクエストへの写しは同じブラウザの中で完結する） |

`README.md` の記述をそのまま写した数字はありません。すべてコードか、実際の画面か、`AUDIT.md` の実測に当たり直しています。

## 撮り直すときの手順

撮影の土台は `note-article` スキルの `scripts/` にあります。

```bash
npm i --no-save playwright
npm run build
node <skill>/scripts/serve.mjs dist 4180 /online-100square-calculation/
node <skill>/scripts/capture.mjs <シナリオ> --base http://127.0.0.1:4180/online-100square-calculation/ --out shots
```

撮影に使ったシナリオは納品時に消してあります。作り直すときに気をつけるところ。

- **解く速さを実時間に合わせる。** 1マス 30 ミリ秒で自動入力すると、記録画面に「100問 14.8秒」というありえないタイムが並びます。1マス 1.3〜1.6 秒にすると、100問で3分半あたりに落ち着きます
- **わざと数問まちがえる。** 全問正解だと「一発で正解できた率」が100%になり、「よく間違えた計算」が何も出ません。くり上がりのある式を狙って外すと、それらしい一覧になります
- **記録画面は、先に何回か解いてから開く。** 空の記録画面には見せるものがありません
- **手書きの画面は、指を離す前に撮る。** 離すと 0.4 秒後に読み取りが走り、線が消えます
- **印刷の画面は幅 703px で撮る。** A4 縦の印刷可能幅です。1366px のまま印刷のスタイルだけ当てても、紙で起きることは再現しません
- ボタンにはふりがなが入っています。文字で押すときは `<rt>` を落としてから比べてください。`capture.mjs` の `click()` はそうしてあります

## 文体について踏襲したこと

`references/style.md` の指定に合わせてあります。

- 太字、斜体、表、区切り記号を本文に使っていない。Markdown は見出しだけ
- 箇条書きは手順のところだけ。それ以外は文章
- 専門用語を日常の言葉に置きかえた。手書きの読み取りのしくみ、記録の保存先、先読みの量など、名前を出さずに書いている
- 絵文字は見出しだけ
- 子どものことは「子どもたち」、読み手は「先生」「先生方」
- 見出しは基本の6つに、4つ足した形。足したのは「✍️ 指で数字を書いて答える」「📊 タイムより『一発で正解できた率』を見てください」「🧠 『毎日おなじ計算をくり返して、意味があるのか』」「🗓️ 学年ごとの目安と、学校でそろえるとき」
- 「導入のメリット」は「三つのいいことが期待できます。」から始める型
- 結びは #9 と同じ「一歩踏み出そうとする先生を、私はいつでも全力で応援しています。」

守ったことのうち、とくに注意したところ。

- **教室での出来事を実体験として書いていない。** この記事は作者の実践記録ではないので、「使ってみたら子どもたちが〜」は書けません。書いてあるのは「こうなればいいと思って作った」までです
- **効果を言い切っていない。** 「〜だと思います」「〜ではないかと思っています」の形にしてあります
- **批判への反論を、研究の引用で押していない。** 「🧠」の節は、100マス計算に向けられる批判（単純作業・思考力・1桁への偏り・誤答の定着）に答えるところですが、
  出典を並べて論破する形にはしていません。「こう位置づけて作った」という設計の説明までにとどめてあります。
  ワーキングメモリという語も使わず、「頭の場所が空く」と書いています
- **学年ごとの目安は、アプリの機能ではなく運用の話として書いてある。** アプリの側に学年の設定はないので、
  「低学年は10〜30問」などは目安であることが本文で分かるようにしてあります
- まとめの最後に「教室で使ってみないと分からないことが残っている」と書いてあります。実際の授業での様子は、書き手が自分で足すところです

## 撮影のあいだに見つかった不具合

ありません。19点すべて、記事に載せた機能を実際に操作して撮っています。手書きの読み取り、提示モード、印刷、記録、中断、すぐ判定の切り替え、入力ツールの左右の入れかえまで通して踏みました。

撮影のために一時的に当てた変更もありません。接続先の差しかえも、`/etc/hosts` の書きかえも使っていないので、`git status` にはこのフォルダの追加しか出ません。
